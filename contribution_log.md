# AI Contribution Log

**Student:** Aftab Momin | **PRN:** 25UAM028 | **Course:** 02AML204

## AI tools used
- Claude (Anthropic)

## What AI helped with
- Writing and reviewing the BFS and A\* Python implementation (`bfs_astar_compare.py`)
- Setting up the profiling code: `time.perf_counter()` timing and manual
  node-expansion counters inside the search loops
- Writing the `py-spy` wrapper scripts (`profile_bfs_run.py`, `profile_astar_run.py`)
  used to generate the flame graphs
- Formatting the final report (`SLE2_25UAM028_AftabMomin.docx`) to match the
  exact structure required in Section 6 of the SLE-2 guideline
- Drafting this README and the flame graph explanation

## What I did myself
- Ran the script and the `py-spy` commands on my own machine
- Verified that BFS and A\* both produce a correct, equal-length path on the maze
- Read and recorded the actual measured numbers (time, nodes expanded) used in the report
- Reviewed the generated flame graphs and confirmed the hotspot lines they point to
- Wrote the final justification, analysis, and conclusion based on my own
  understanding of the results (not copied from AI output verbatim)
- Filled in personal/course details (PRN, name, date) and proofread the final report
